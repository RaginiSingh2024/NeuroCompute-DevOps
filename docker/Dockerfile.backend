# Backend Dockerfile
FROM node:18-alpine
WORKDIR /usr/src/app
COPY backend/package.json backend/package-lock.json ./
RUN npm ci --production
COPY backend/ .
ENV NODE_ENV=production
EXPOSE 5000
CMD ["node", "server.js"]
